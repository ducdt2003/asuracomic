package com.example.asuracomic.model.enums;

public enum ReportContentType {
    COMIC,    // Báo cáo nội dung truyện (VD: vi phạm bản quyền, nội dung không phù hợp)
    CHAPTER,  // Báo cáo chương cụ thể (VD: lỗi ảnh, nội dung sai lệch)
    COMMENT   // Báo cáo bình luận (VD: xúc phạm, spam), hỗ trợ kiểm duyệt cộng đồng
}
